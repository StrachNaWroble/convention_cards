# WBF Card Layout

Low-level layout primitives for recreating the WBF card grid live here.

The layout should be fixed-format for desktop and print, with stable row and column dimensions so editing text does not shift the card structure.
