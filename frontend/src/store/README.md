# Store (Application State)
A place for the logic of storing data in the browser's memory. We keep the dynamically filled table fields here (e.g., entries from the OVERCALLS or DOUBLES sections)[cite: 1] before sending them to the database, to prevent losing work.
