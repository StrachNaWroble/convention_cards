# Shared Card View

Public and partner-only read-only card views live here.

Shared cards should expose player names and WBF numbers, and should render the same card layout used by the editor and print flow.
