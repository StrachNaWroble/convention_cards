# Editor View

The editor screen hosts the interactive WBF card canvas.

The first desktop version should prioritize a faithful printable card layout with transparent field editing, autosave state, validation feedback, partner approval flow, print, and PDF export actions.
