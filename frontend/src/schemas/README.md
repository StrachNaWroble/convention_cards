# Schemas

Shared frontend schema definitions live here.

The WBF card schema should describe structured field data for validation, autosave, rendering, print, export, and cloning.

Keep schema names aligned with backend validation concepts so the frontend can show useful feedback before the backend enforces lifecycle changes.
